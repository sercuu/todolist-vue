const state = {
    todolist: {
        title:'ss'
    }
}
const getters = {
    todolist: (state) => state.todolist
}
const mutations = {}
const aciotns = {}

export default {
    state,
    getters,
    mutations,
    aciotns
}